#ifndef MFrequence_h
#define MFrequence_h

#include <Arduino.h>

class MFrequence {
  public:
    //MFrequence();
    //void begin();
    //void get();


private:
    // void initTimer(uint8_t ID, uint16_t Prescaler, uint16_t alarm);
    // void enableAlarm();
    // void disableAlarm();
};

#endif